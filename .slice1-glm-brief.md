# Mecha-Lit Slice 1 — PySide6 采集台（你＝GLM 执行）

工作目录就是你被启动的 `D:\mecha-lit`。建一个 **PySide6 原生桌面 App（不是 webview、不是浏览器）**，结构/QSS/线程模式严格对齐 `D:\mecha-quant`。

## 动手前必读（别跳）
1. `D:\mecha-lit\SPEC.md` —— 完整规格，**按 §7 Slice-1 范围建**（本 brief 不重复 SPEC，只补 delta）。
2. `D:\mecha-lit\brief.md` —— PI 的关键点（血的教训）。
3. 照 `D:\mecha-quant` 的写法建，先读：`gui.py`、`ui\main_window.py`、`ui\workers.py`、`ui\style.py`、`ui\state.py`、`ui\pages\data_center.py`（后台任务+进度条范例页）。**只读参照，一个字节都不改 mecha-quant / Mecha-Core。**

## 已就位脚手架（别重建）
- `ui/style.py`（QSS 常量 + 主题，已从 mecha-quant 复制，配色用它的常量 ACCENT/TEXT/MUTED/BORDER/CARD_BG…）
- `ui/__init__.py`、`lit/__init__.py`、`ui/pages/__init__.py`（空包标记）
- `prototype-reference.html`（采集台设计/流程参照，翻成 Qt 控件）

## 必改（否则 import 即崩）
`ui/workers.py` 顶部 `from quant import datahub, scan` 和 `import pandas as pd` 是 mecha-quant 专用，本项目没有 quant 包。**删掉这两行 + 删 UpdateWorker/EventOpWorker/ScanWorker/BatchWorker 四个类**，只留通用 `FuncWorker` + `run_async`（这两个照抄别动）。

## 要建的文件（SPEC §2 的具体化）
- `gui.py`：抄 mecha-quant/gui.py 骨架（QApplication + `app.setStyleSheet(style.QSS)` + `SetCurrentProcessExplicitAppUserModelID("MechaLit.App")` + MainWindow）。本项目无 quant 包、无 wheel_guard，别 import 它们。
- `lit/config.py`：`APP_NAME="机甲文献"`、`VERSION="0.1.0"`、`ROOT=Path(__file__).resolve().parent.parent`、`ENGINE_PATH=Path(r"D:\BaiduSyncdisk\Mecha-Core\scripts\zotero-import.ps1")`、`MECHA_CORE=Path(r"D:\BaiduSyncdisk\Mecha-Core")`。
- `lit/engine.py`：
  - `run_search(journal, *, reldate_days=None, month=None, year=None, include_editorial=False, include_letter=False, topic_filter=None, timeout=180) -> dict`
  - 构造 argv：`["powershell","-NoProfile","-ExecutionPolicy","Bypass","-File",str(ENGINE_PATH),"-Journal",journal]`，三选一加 `-ReldateDays <int>` / `-Month <YYYY-MM>` / `-Year <int>`，按开关加 `-IncludeEditorial`/`-IncludeLetter`/`-TopicFilter <s>`，**末尾固定加 `-EmitJson`，绝不加 `-Execute`**。
  - `subprocess.run(argv, capture_output=True, text=True, encoding="utf-8", errors="replace", timeout=timeout)`；非 0 退出或超时 → 抛带 stderr 的清晰异常。
  - 解析：遍历 `stdout.splitlines()`，取以 `MECHA_JSON ` 开头的那行，`json.loads(line[len("MECHA_JSON "):])` 返回 dict；找不到该行 → 抛异常（附 stdout 末尾 500 字辅助诊断）。
- `ui/main_window.py`：左 `QListWidget#nav`（fixedWidth≈168，三项「📡 采集台 / ⚙️ 设置 / 📖 使用说明」）+ 右 `QStackedWidget`；`closeEvent` 里 wait 各页 `_workers`（照 mecha-quant）。
- `ui/pages/harvest_page.py`（采集台＝核心页，照 prototype 翻 Qt）：
  - 左：分类期刊树。Slice 1 只放"胸部肿瘤与胸外科"10 本静态刊（prototype 里那 10 本，J Thorac Oncol 默认选中）。用 QTreeWidget（分类节点 + 子刊物）或 QListWidget 都行，单选。
  - 检索配置 chips：Article/Review/Editorial/Letter（可勾，默认 Article+Review+Editorial 勾、Letter 不勾）+「仅要有摘要」（默认勾）。Slice 1 **只做 UI 状态、不写回 journal-overrides.json**（那是 Slice 2）。
  - 模式：Slice 1 只实现「采集最新」（-ReldateDays，默认 60 天）。回填历史可留占位/禁用。
  - 「检索」按钮：**pubtype 至少勾一个才启用**；点击 → 禁用按钮 + 显示"检索中…（约 30–60 秒）" → `run_async` 跑 `lit.engine.run_search(选中刊, reldate_days=60, include_editorial=Editorial勾, include_letter=Letter勾)` → done 回调渲染审计页；failed 回调显示错误原文。
  - 审计页（照 prototype 的 receipt 区 + SPEC §4）：时间戳（datetime.now()）、最终 query（`r["query"]`）、统计行 found/new/dup/suspect（`r["found"]`、`r["counts"]`）、按 status 分组清单（`r["items"]`：每条 title/type/status/hasAbstract/pmid/doi）。new 与 dup 分两组，用卡片样式，配色用 style.py 常量。
- `ui/pages/settings_page.py`、`ui/pages/help_page.py`：最小占位（pageTitle + 几行说明文字）。
- `requirements.txt`：`PySide6>=6.7`

## 引擎契约（主驾已实测，勿改引擎）
dry-run（无 -Execute）stdout 末行形如：
`MECHA_JSON {"schema":"mecha-lit/import-result@1","journal":"J Thorac Oncol","mode":"reldate-60","query":"...","executed":false,"found":31,"counts":{"new":19,"dup":12,"suspect":0,"imported":0,"failed":0},"collection":{"key":"2HID2MQK","exists":true},"items":[{"pmid":..,"doi":..,"title":..,"type":..,"hasAbstract":..,"status":"new|dup","dedupBy":..,"dupSrc":..}]}`
**主驾实测 ground truth**：`J Thorac Oncol -ReldateDays 60` → **found=31 new=19 dup=12 suspect=0**。这是你验证桥是否真通的标准（逐日会变几篇，found 落在 25–40 区间即正常）。
注意：PowerShell stdout 可能有 BOM / 混合换行，parse 前 `.strip()`、按行 split。

## Slice 1 护栏（SPEC §7 基础三条）
①检索走 workers 线程（`run_async`），**绝不卡 UI**（引擎 30–60s）②运行中禁用「检索」按钮 + 显示进度文案③pubtype 至少勾一个否则禁检索。
**绝不 `-Execute` / 任何真实 Zotero 写入。**

## 严禁
不改 `D:\mecha-quant`、不改 `D:\BaiduSyncdisk\Mecha-Core`（只读参照 + 只读 spawn 引擎）；不重写引擎为 Python（一律 spawn PS）；Slice 1 禁 -Execute；不引 CDN、App 不自行联网（引擎负责 PubMed/Zotero）。

## 自验（必须真跑，不只写代码）
1. `python -m venv venv`
2. `venv/Scripts/python.exe -m pip install -r requirements.txt`
3. 起窗自检（GUI 按钮你点不了，用自检替代）：`venv/Scripts/python.exe -c "import os,sys; os.environ['MECHA_SELFTEST']='1'; sys.path.insert(0,'.'); import gui; import gui as g; g.main() and None"`——能跑过 QApplication+MainWindow 建窗、3 秒自退（照 mecha-quant gui.py 的 MECHA_SELFTEST 机制）不崩即过。或更稳：写个 3 行脚本 `QApplication([]); from ui.main_window import MainWindow; w=MainWindow(); w.show()` 配 `QTimer.singleShot(2000, app.quit)` 跑，exit 0 即起窗成功。
4. **桥探针（证明 spawn+parse 真通）**：
   `venv/Scripts/python.exe -c "from lit.engine import run_search; r=run_search('J Thorac Oncol', reldate_days=60); print('found=%s counts=%s items=%d' % (r['found'], r['counts'], len(r['items'])))"`
   预期：`found=31（±几） counts={'new':19,'dup':12,'suspect':0,'imported':0,'failed':0} items=31`。**found 落 25–40、counts 各项≥0、items 非空即桥通。**

## 失败处理
预期外即停、贴报错原文 + 你的定位；不擅自 -Execute、不碰引擎/mecha-quant/Mecha-Core、不擅自扩范围。

## 交付（≤400 字最终消息）
①gui 起窗自检是否过（exit 0）②桥探针真实 found/new/dup 数 ③venv+requirements 是否装好 ④卡点（贴报错原文）⑤文件清单 + 起动步骤。另写 `BUILD-NOTES.md`（起动步骤 + 各功能实现位置 + 桩点清单）。你的最终消息＝交付回执。
