# Trackeep Lit · 阶段日志

## 当前活跃状态（2026-07-18 收工）

**v0.2.0 全部收口，项目转入 R2 规范治下待命**。本日一次会话完成：品牌重构（机甲文献→
Trackeep Lit，含 wire 层 TRACKEEP_JSON/schema/tool 全迁）→ 生产规范 v1.1.2 全套落地
（合同/AGENTS/五件套/GitHub 私有仓）→ 测试从 0 建到五件套 **122 项**（smoke 51/gui 14/
stress 5×50 轮/adversarial 47/property 5×500 例）→ 对抗轮挖修 **11 硬缺陷**（VS-11）→
CI 四门禁全绿（末笔 dd51d60）→ 打包收尾（onedir 112MB，frozen selftest exit 0）→
分级升 **S1/R2/C1**。功能面自 Slice 6b-1 起冻结未动。

**等 PI 拍板**：①GitHub Pro（约 $4/月）→ 补设分支保护（EX-01 到期条件）②日常入口留 VBS
（venv 开发态）还是切打包 exe。

**下轮开工清单**（在 R2 门禁内走母法 4.7）：6b-2 DeepSeek 复筛真拦截（BL-04，keep 语义已修好
铺垫）· 变异测试首轮（BL-08）· UI 随机事件风暴 + 对抗软观察（BL-02/07）。

## 阶段路线图

| 阶段 | 内容 | 状态 |
|---|---|---|
| Slice 1 | PySide6 采集台核心（引擎桥 + dry-run 审计页） | ✅ 2026-07-13 |
| Slice 2 | 全 74 刊载入 + 例外表写回 + 台账自适应窗口（修 BOM 静默 bug） | ✅ 2026-07-13 |
| Slice 3 | 真实导入（-Execute）+ 受控建 collection + 幂等重试（JTO 6 篇端到端） | ✅ 2026-07-13 |
| Slice 4 | 回填历史（月/年）+ retmax 告警 + VBS 启动器 + 设置/说明页 | ✅ 2026-07-13 |
| Slice 5 | PyInstaller 打包 | ⏸ spec 就位，装法待 PI（BL-03） |
| Slice 6a | 采集策略页（按分类 pubtype/主题过滤/DeepSeek 判据）+ strategy.json + resolve 契约 | ✅ 2026-07-13 |
| 6b-1 | DeepSeek 复筛 advisory（只显不拦） | ✅ 2026-07-13 |
| 重构 | 采集台并入采集策略（左树单脊柱） | ✅ 2026-07-13 |
| **v0.2.0** | **品牌重构（Trackeep Lit）+ 生产规范 v1.1.2 全套落地（合同/五件套/测试两件套/CI）** | 🔨 2026-07-18 |
| 6b-2 | DeepSeek 复筛真拦截（导入门控 + 捞回交互） | 排下轮（BL-04） |

## 调整记录

### 2026-07-18 · v0.2.0 品牌重构 + 生产规范落地（本轮）

- **PI 四项拍板**：①分级 S1/R1/C1（R2 论点已暴露，裁 R1）②建 GitHub 私有仓 + CI 四门禁（照搬
  Trackeep Quant 方子）③先落规范再续功能 ④品牌改名 Trackeep Lit（退役词登记已把「mecha」收窄为
  仅指内部 AI 联邦，跟进 Trackeep Quant 先例）。
- **品牌迁移**（GLM 施工、主驾验收，commit 2000b52）：目录 D:\mecha-lit→D:\trackeep-lit、
  APP_NAME/VERSION、MECHA_SELFTEST→TRACKEEP_SELFTEST、vbs/spec 改名。（wire 契约当时暂留旧名，
  同日被下一条推翻。）
- **wire 层随品牌全迁**（PI 2026-07-18 追加拍板，GLM 施工、主驾审核）：`MECHA_JSON `→
  `TRACKEEP_JSON `、schema `mecha-lit/import-result@1`→`trackeep-lit/import-result@1`、NCBI
  `tool=mecha-lit`→`trackeep-lit`——引擎（zotero-import.ps1，5 处）与 App/tests 两端原子切换，
  引擎唯一消费方=本 App、无兼容包袱。审核链：diff 对备份恰 5 行 + 两端零残留 grep + 两件套
  亲跑全绿 + **端到端真跑 dry-run 验证**（真引擎吐新前缀、schema 新值、executed=False 零写入）。
  引擎改前备份存 `archives/zotero-import.ps1.bak-20260718-pre-wire-rename`（Mecha-Core 无 git，
  此为回滚件；hash c92067cd…5636）。
- **规范落地**：PROJECT-CONTRACT.md + AGENTS.md（红线内联）+ CLAUDE.md + .project 五件套
  （policy 12+2 义务 / invariants 11 条 / vulnerable-scenarios 10 条 / exceptions 3 条 / manifest）。
  历史施工文档（SPEC/SLICE*/brief/EXECUTE-NOW/BUILD-NOTES/prototype-reference）归档 archives/。
- **测试两件套**（GLM 施工、主驾独立四跑验收）：smoke 51 项（引擎 mock/失败注入/脏数据/
  resolve 契约/凭证不泄）+ gui 14 项（确认闸/单飞/锁定参数/泄漏/告警），断言源=invariants.yaml；
  Mecha-Core 三 json mtime 前后一致（零真写实证）。
- **CI 首跑两红、均非产品缺陷**：①tests——smoke 的 VS-04 检查写死 Windows 假设
  （`CREATE_NO_WINDOW` 仅 win32 有），改平台分支断言（非 Windows 断言 getattr 兜底 0）；
  ②secret-scan——gitleaks 本体报 **no leaks found**，exit 1 是 gitleaks-action 首推 quirk
  （拿初始 commit 父提交算范围、父不存在→partial scan 报错），后续正常 push 范围合法即自愈。
  教训入账：测试断言不得隐含平台假设（CI=ubuntu，本地=Windows，两边都要绿）。

### 2026-07-18 · 测试军团轮：3×GLM 并行 + 对抗挖洞 + 修复轮（PI 拍板「多派 GLM 测试、并发升档」）

- **3×GLM worktree 隔离并行**（主驾逐个审后 octopus 合并）：
  - GLM-A `stress_test.py`（5 项）：连点/乱点/渲染清空 ≥50 轮 + 随机时序抖动（seed 入证据）——
    并发档 10→50 **升档回母法档**；未发现产品竞态缺陷；主驾追加 drain 后终值断言加固。
  - GLM-B `adversarial_test.py`（47 项）：主动证伪挖出 **11 处硬缺陷**（engine/deepseek 原始
    JSONDecodeError 未转人话、keep 字符串 'false' 误判留、harvest found/payload/collection
    类型畸形崩溃）+ 2 条软观察（BL-07）。
  - GLM-C `property_test.py`（5 属性 ×≥500 例）：resolve 合并/解析/往返/窗口全部不变式无违反，
    双 seed 复现——**BL-01 属性测试落地**。
- **修复轮**（GLM 施工、主驾独立复现核证 + diff 审 + 五套亲跑）：11 硬缺陷 + GLM-A 相邻观察
  （FuncWorker 不接 BaseException 致 UI 永卡）共 12 项全修，定级 B2 批量；adversarial
  35/46→**47/47 转绿**；合法路径行为零改变（四套既有测试护栏全绿）。触发路径入表 VS-11。
- **CI 升级**：tests job 挂五套（122 项断言）+ 相等门禁扩至五键 + seed 入 test-summary/evidence。
- 治理同步：policy（R1-06/并发档/对抗测试 enabled）、合同 §7、invariants 证据从 backlog 转实、
  manifest（BL-01 resolved / DEFECT-ROUND-1 / BL-07 新增）。

### 2026-07-18 · 第二批拍板落地：R2 升级 + 打包收尾 + 分支保护实测（PI 四项指令）

- **分支保护**：classic branch protection 与 rulesets 双路 API 实测均 403「Upgrade to GitHub Pro
  or make this repository public」——Free 私有仓付费墙确证，EX-01 补实测证据；升 Pro（约 $4/月）
  属新增付费服务，待 PI 拍板，未擅自动。
- **仓名单 K**：核实 GitHub 实名 `trackeep-lit`（账号下 trackeep 系三仓全单 k）+ 仓内零双 k
  残留——已符合，无需改。
- **打包收尾（BL-03 关闭）**：PI 授权绕过安装钩子（先查现成：系统 pip 与 quant venv 均有
  PyInstaller 6.21.0，lit venv 无）→ 钉同版本装入 lit venv（走 hook 放行缓存、留 PI 授权审计标记）
  → `Trackeep Lit.spec` 打包 onedir 112MB → **frozen selftest exit 0**。PyInstaller 属构建工具，
  不入 requirements-lock（CI 不需）；dist/build 不入库（.gitignore 既有）。
- **分级升 R2**（PI 拍板，采纳先前暴露的 R2 论点：文献库=重要数据资产+持写凭证）：合同 §1 判级
  更新 + R2 义务落点成文；policy 新增 R2 段 9 条（独立复核/对抗每候选版本/故障注入/安全隐私/
  事故响应/证据包/需求追踪=enabled，迁移演练=n/a，变异测试=backlog BL-08）；AGENTS §5 复核方式
  升 R2 口径。R2 新增义务中唯一未落地件=变异测试首轮（BL-08，下个发布周期）。

### 2026-07-13 · Slice 施工期（v0.1.0）

Slice 1→6b-1 + 重构，10 个 commit，全部主驾监督 GLM 施工、逐片验收（详见 git log 与 archives/ 施工单）。
首次真实导入 JTO 6 篇端到端验证（imported=6 / 幂等重试 / 锚点前移全通过）。
